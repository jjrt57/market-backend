name: Diagnostic Test Run

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World Diagnostic
        run: echo "The GitHub Actions engine is working perfectly."
